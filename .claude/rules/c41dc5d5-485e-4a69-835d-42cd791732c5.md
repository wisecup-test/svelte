<rule_activation id="c41dc5d5-485e-4a69-835d-42cd791732c5" title="Adopt Modular Public API Export Pattern with Explicit Re-exports: Exported Functions Classes" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching the modular public API export pattern across framework entry points, compiler utilities, and runtime functions.
</rule_activation>

### Rules

- **R-API-001** MUST: All exported functions and classes MUST be documented with JSDoc comments indicating their public API status.

### Verify

```bash
# Count re-export patterns from internal modules in entry points
grep -r "export.*from.*internal" packages/*/src/index*.js | wc -l

# Find entry point files that lack export statements
find packages/*/src -name 'index*.js' -exec grep -L 'export.*from' {} \;

# Verify build succeeds and produces non-empty API surface
npm run build && node -e "const api = require('./dist'); console.log(Object.keys(api).length > 0)"
```

**Accept when:**
- All public entry point files contain only re-export statements without implementation logic
- Internal implementation files are organized in separate directories from entry points
- Build succeeds and produces separate bundles for different targets (client, server, dev) when applicable
- Public API documentation can be generated solely from entry point files

<enforcement>
Claude Code MUST NOT skip or defer verification. Entry point structure and export patterns are verified by automated CI checks, code review checklists, linting rules, and API documentation generation processes. Violations result in CI build failures and blocked pull requests.
</enforcement>