<rule_activation id="9453e77a-c975-4696-97c4-ee9ed0868860" title="Standardize Public API Export Patterns for External Integration: Public Exports Follow" applies_to="**/*">
These rules are ALWAYS ACTIVE for all modules in packages/svelte/src/internal/client/*, packages/svelte/src/compiler/*, and core framework APIs that expose functionality to external consumers.
</rule_activation>

### Rules

- **R-API-001** MUST: Public API exports MUST follow the detected pattern signature f8cf7f20e7f18319e094d3c7c19a989d for service boundary definitions.
- **R-API-002** MUST: All public API modules in scope have explicit export statements that match the detected pattern.
- **R-API-003** MUST: No internal implementation details are exported without @public or @experimental documentation tags.
- **R-API-004** SHOULD: Prioritize refactoring high-traffic APIs first when aligning existing modules with the pattern.
- **R-API-005** MAY: Framework maintainers may expose experimental APIs for early adopter testing (EXC-001).

### Verify

```bash
# Count explicit export statements in client and compiler modules
grep -r 'export.*from' packages/svelte/src/internal/client packages/svelte/src/compiler | grep -v '.test.js' | wc -l

# Find modules using explicit export syntax
find packages/svelte/src -name '*.js' -path '*/internal/*' -exec grep -l 'export {' {} \; | wc -l

# Lint for export boundary violations
npm run lint -- --rule 'no-restricted-syntax: [error, ExportAllDeclaration]'
```

**Accept when:**
- All public API modules in scope have explicit export statements that match the detected pattern
- No internal implementation details are exported without @public or @experimental documentation tags
- Linting passes with no violations of export boundary rules
- API documentation clearly distinguishes public vs internal APIs for all modules in scope

<enforcement>
Claude Code MUST NOT skip or defer verification. All public API exports MUST be validated against the pattern signature f8cf7f20e7f18319e094d3c7c19a989d before acceptance. Violations trigger CI build failure and require architecture team review.
</enforcement>