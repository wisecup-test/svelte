<rule_activation id="675b6f8e-a71a-4de9-88b9-65a4de8c2da4" title="Adopt Modular Public API Export Pattern with Explicit Re-exports: Public Modules Use" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching public API entry points and framework exports.
</rule_activation>

### Rules

- **R-API-001** SHOULD: Public API modules SHOULD use named exports rather than default exports to improve tree-shaking and IDE support.

### Verify

```bash
# Verify entry points contain only re-export statements
grep -r "export.*from.*internal" packages/*/src/index*.js | wc -l

# Find entry point files that lack export statements
find packages/*/src -name 'index*.js' -exec grep -L 'export.*from' {} \;

# Verify build succeeds and produces API surface
npm run build && node -e "const api = require('./dist'); console.log(Object.keys(api).length > 0)"
```

**Accept when:**
- All public entry point files contain only re-export statements without implementation logic
- Internal implementation files are organized in separate directories from entry points
- Build succeeds and produces separate bundles for different targets (client, server, dev) when applicable
- Public API documentation can be generated solely from entry point files

<enforcement>
Claude Code MUST verify all rules before accepting changes to public API entry points. Verification is mandatory and cannot be deferred.
</enforcement>