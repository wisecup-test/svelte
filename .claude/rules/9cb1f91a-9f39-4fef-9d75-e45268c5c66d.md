<rule_activation id="9cb1f91a-9f39-4fef-9d75-e45268c5c66d" title="Standardize Public API Export Patterns for External Integration: Public Modules Define" applies_to="packages/svelte/src/internal/client/**/*.js,packages/svelte/src/compiler/**/*.js">
These rules are ALWAYS ACTIVE for all public API modules in the Svelte framework that expose functionality to external consumers. Pattern detected across 4 critical files (async.js, css.js, warnings.js, context.js) with 90% confidence.
</rule_activation>

### Rules

- **R-API-001** MUST: All public API modules MUST define explicit export boundaries that separate internal implementation from external interface.
- **R-API-002** MUST: No internal implementation details are exported without @public or @experimental documentation tags.
- **R-API-003** MUST: All public exports in scope modules (packages/svelte/src/internal/client/*, packages/svelte/src/compiler/*) MUST use explicit export statements (not export *).
- **R-API-004** SHOULD: Public APIs SHOULD be documented in contributor guidelines with examples from detected pattern files.
- **R-API-005** MAY: Experimental APIs MAY be exposed for early adopter testing with explicit @experimental tags (EXC-001).

### Verify

```bash
# Count explicit export statements in public API modules
grep -r 'export.*from' packages/svelte/src/internal/client packages/svelte/src/compiler | grep -v '.test.js' | wc -l

# Find modules using explicit export syntax
find packages/svelte/src -name '*.js' -path '*/internal/*' -exec grep -l 'export {' {} \; | wc -l

# Lint for export boundary violations (reject export *)
npm run lint -- --rule 'no-restricted-syntax: [error, ExportAllDeclaration]'
```

**Accept when:**
- All public API modules in scope have explicit export statements that match the detected pattern
- No internal implementation details are exported without @public or @experimental documentation tags
- Linting passes with no violations of export boundary rules
- API documentation clearly distinguishes public vs internal APIs for all modules in scope
- The 4 detected files (async.js, css.js, warnings.js, context.js) are audited and documented as pattern references

<enforcement>
Claude Code MUST NOT skip or defer verification. All public API modules matching the applies_to pattern MUST pass linting and documentation checks before acceptance. Violations trigger CI build failure and require architecture team review.
</enforcement>